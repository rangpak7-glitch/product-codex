import type { Metadata } from "next";
import { PageHero, Section } from "../../components/site-shell";
import { resources } from "../../lib/content";
export const metadata: Metadata={title:"신앙자료"};
export default function Page(){return <><PageHero eyebrow="신앙자료" title="오늘 마음에 가까운 기도 자료를 찾아보세요" description="기도문 PDF, 기도 오디오북, 기도카드를 공개 미리보기로 먼저 확인할 수 있습니다." actions={<a className="button" href="#catalog">자료 찾아보기</a>} /><Section eyebrow="형식과 상황" title="어떤 자료가 필요하세요?" id="catalog"><div className="tagRow"><a className="tag" href="/prayer-cards.html">전체</a><a className="tag" href="/prayer-cards.html?type=pdf">기도문 PDF</a><a className="tag" href="/prayer-cards.html?type=audio">기도 오디오북</a><a className="tag" href="/prayer-cards.html?type=card">기도카드</a></div><div className="resourceGrid">{resources.map((resource)=><article className="card" key={resource.id}><p className="eyebrow">{resource.type}</p><h3>{resource.title}</h3><p>{resource.summary}</p><ul>{resource.previewItems.map((item)=><li key={item}>{item}</li>)}</ul><a href={`/prayer-cards.html?resource=${resource.resourceId}`}>공개 미리보기</a><a href={`/contact.html?product=${resource.id}`}>자료 문의하기</a></article>)}</div></Section></>}
